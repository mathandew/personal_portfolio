import { createRouter, createWebHistory } from "vue-router";

const routes = [
  // Add routes if needed for multi-page functionality
];

const router = createRouter({
  history: createWebHistory(),
  routes,
});

export default router;
